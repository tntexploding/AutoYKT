# 变更记录

## 0.2.0 — 2026-09-20

本阶段将原型整理为可配置的 Windows 课堂答题流程，后续以实际使用反馈和修复为主。

- 严格 v2 私有配置、旧配置迁移、可安装 CLI；公开仓库移除个人题型和选项截图，只保留模板与合成测试。
- 窗口与页面身份保护；单选、多选、动态选项、逐项选中确认、提交反馈、新题提示和换题去重。
- Flash 限时并发采样，初始 3 次、最多 5 次；完整答案集合投票、无效回答过滤及预算内重试。
- 两小时课堂启动器、OCR 预热、状态/停止命令、实际监测统计、操作证据和重启检查点。
- 课程资料导入、本地分课程检索、实时课件 OCR；可选通知默认关闭。
- 原尺寸图片检查、标注样本验证和虚拟完整流程回放。
- 标定支持 A–Z，新增交互式选中/未选中颜色及完整按钮尺寸采集。
- 修复指定 profile 检查错范围、空私有密钥、正常换题等待误报、结束报告漏计晚到错误。
- 将 Python 安装范围约束为 3.10–3.12，推荐 3.12，解决 OCR 依赖在干净 3.13/3.14 环境无法安装的问题；依赖以 `pyproject.toml` 为唯一维护来源。
- 重写快速开始，补充首次标定、环境变量持久化、恢复和更新文档。

### 升级说明

升级前停止所有控制进程，备份整个私有目录。配置 `version: 2` 保持不变，新字段有默认值。旧版未版本化配置需先 `migrate`；导出会去掉密钥，需重新指定来源。现有模板不随代码更新被覆盖。

如原虚拟环境是通过跳过依赖要求创建的 Python 3.13/3.14 环境，请保留备份并用 Python 3.12 新建 `.venv`，按 README 安装；不要依赖 `--ignore-requires-python` 进行全新部署。

`start-class.cmd` / `session` 会启用当次真实点击；`--dry-run` 仅预览。完成状态只证明匹配到提交反馈，不证明答案正确。实际平台提交和连续两小时运行仍应按 [正式验收](docs/formal-page-operations.md) 记录。
